import { HttpInterceptorRequest } from './http-interceptor-request';

describe('HttpInterceptorRequest', () => {
  it('should create an instance', () => {
    expect(new HttpInterceptorRequest()).toBeTruthy();
  });
});
